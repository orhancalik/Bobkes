<!DOCTYPE html>
<html lang="nl-BE">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../css/reset.css" />
    <link rel="stylesheet" href="../css/style.css" />

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>Who's that Pokemon?</title>
  </head>
  <body>
    <header>
      <nav>
        <ul class="nav_links">
          <li>
            <a href="index">
              <img
                src="assets/images/Logo.png"
                alt="logo"
                width="175px"
                height="175px"
              />
            </a>
          </li>
          <li><a href="mijnpokemon">Mijn Pokemon</a></li>
          <li><a href="pokemonvergelijken">Vergelijken</a></li>
          <li><a href="pokemonbattler">Battler</a></li>
          <li><a href="pokemoncatcher">Catcher</a></li>
          <li><a href="whosthatpokemon">Who's that pokemon</a></li>
          <% if (user) { %>
          <li>
            <form id="logoutForm" action="/logout" method="POST">
              <button type="submit">Logout</button>
            </form>
          </li>
          <% } else { %>
          <li><a href="login">Login </a></li>
          <% } %>
          <li class="pokeball">
            <img src="/assets/images/pokeball.png" alt="" width="50px" />
            <div class="pokemon-info">
              <% if (user && user.username) { %>
              <span id="currentPokemonName"><%= user.username %></span>
              <% } else { %>
              <span id="currentPokemonName">Vang pokemon</span>
              <% } %> <% if (user && user.pokemon) { %>
              <span class="<%= user.pokemon.type %>"
                ><%= user.pokemon.name %></span
              >
              <% } else { %>
              <span class="no-pokemon">Geen pokemon</span>
              <% } %>
            </div>
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <section id="who-page">
        <article class="projecten-item">
          <h1>Who's that Pokémon?</h1>
          <img
            id="blurry"
            src="<%= pokemon.image %>"
            alt="Who's that Pokémon"
          />
          <form id="guessForm" method="POST" action="/whosthatpokemon">
            <label for="pokemonName">Raad de naam van de Pokémon:</label>
            <input type="text" id="pokemonName" name="pokemonName" required />
            <input
              type="hidden"
              name="correctName"
              value="<%= pokemon.name %>"
            />
            <button class="myBttn" type="submit">Indienen</button>
          </form>
        </article>
        <article class="projecten-item">
          <h1>Huidige Pokémon</h1>
          <img
            src="assets/images/bulbasaur.jpg"
            alt="Current Pokémon"
            width="350px"
          />
          <div class="progress" style="height: 30px">
            <div
              class="progress-bar bg-warning"
              role="progressbar"
              style="width: 100"
              aria-valuenow="<%= currentPokemon.attack %>"
              aria-valuemin="0"
              aria-valuemax="100"
            >
              Aanval: <%= currentPokemon.attack %>
            </div>
          </div>
          <div class="progress" style="height: 30px">
            <div
              class="progress-bar bg-danger"
              role="progressbar"
              style="width: 100"
              aria-valuenow="<%= currentPokemon.defense %>"
              aria-valuemin="0"
              aria-valuemax="100"
            >
              Verdediging: <%= currentPokemon.defense %>
            </div>
          </div>
        </article>
      </section>
    </main>
    <%- include("./partials/footer") %>
  </body>
</html>
